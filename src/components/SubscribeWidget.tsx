import React from 'react';

export default function SubscribeWidget({ 
  variant = 'desktop', 
  mode = 'top' 
}: { 
  variant?: 'desktop' | 'mobile'; 
  mode?: 'top' | 'bottom'; 
}) {
  return null;
}
